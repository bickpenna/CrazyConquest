#include "server.h"
#include "../common/utils.h"
#include <stdio.h>

int server_init(int port) {
    (void)port;
    /* Socket initialization stub */
    return 0;
}

void server_run(int server_fd) {
    (void)server_fd;
    /* Non-blocking main loop stub */
}

void server_cleanup(void) {
    /* Cleanup stub */
}
